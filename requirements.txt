# HireStreamAI — Optimized Python Dependencies for Vercel
# (Removing heavy ML libraries to fit in 500MB Lambda limit)

# API Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.7.0
python-dotenv>=1.0.0
loguru>=0.7.0

# Database
motor>=3.3.0

# Light Text Processing
# (Replacing heavy ML with lighter alternatives or API calls)
spacy-pkuseg>=0.0.32
# spacy==3.7.4 (Will be installed by model if needed)
# scikit-learn (Can be large, removing if not critical for basic flow)

# Document Parsing
PyMuPDF>=1.24.0
python-docx>=1.1.0
Pillow>=10.0.0
