# HireStreamAI — Optimized Python Dependencies for Vercel
# Removed heavy ML libraries (torch, spacy, xgboost) to fit in Lambda limits.
# The app will automatically fall back to TF-IDF and regex-based parsing.

# Core Utilities
numpy>=1.26.0
scikit-learn>=1.4.0

# Text Processing
nltk>=3.8.0
regex>=2023.12.0

# Document Parsing
PyMuPDF>=1.24.0          # PDF parsing (fitz)
python-docx>=1.1.0       # DOCX parsing

# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9  # File uploads
pydantic>=2.7.0

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.0
tqdm>=4.66.0
joblib>=1.4.0

# Database
motor>=3.4.0             # Async MongoDB driver
dnspython>=2.6.0         # Required for mongodb+srv URIs

# Dev / Testing
httpx>=0.27.0            # FastAPI test client
